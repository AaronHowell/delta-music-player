# ---- core: MIDI -> performance (Phases 1-4) ----
mido>=1.3

# ---- dev / test ----
pytest>=8.0
numpy>=1.26          # demo-WAV generation & probe tools only

# ---- audio extraction (DEFERRED, optional) ----
# MELODIA backend: runs inside WSL (essentia has no Windows wheels).
#   Setup: see audio/wsl/README.md  (offline wheel install, no WSL network needed)
#
# basic-pitch backend (Windows-native via ONNX; tensorflow is NOT installable
# on Python 3.14, so install basic-pitch without deps + the ONNX stack):
#   pip install basic-pitch --no-deps
#   pip install onnxruntime librosa resampy scikit-learn pretty-midi mir-eval typing-extensions soundfile
#
# Demucs vocal separation (optional pre-step):
#   pip install demucs        (pulls torch; cp314 Windows wheels exist)
